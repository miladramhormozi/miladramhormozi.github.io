# section information
section:
  name: Recent Posts
  id: recent-posts
  enable: false
  weight: 7
  showOnNavbar: flase
  # Can optionally hide the title in sections
  # hideTitle: true

# no additional configuration is required
